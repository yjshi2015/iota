// Copyright (c) 2025 IOTA Stiftung
// SPDX-License-Identifier: Apache-2.0

syntax = "proto3";

package iota.grpc.v0.events;

import "common.proto";

service EventService {
  rpc StreamEvents (EventStreamRequest) returns (stream iota.grpc.v0.common.Event);
}

message EventStreamRequest {
  EventFilter filter = 1;
}

// Rich event filter that supports gRPC event filtering
message EventFilter {
  oneof filter {
    iota.grpc.v0.common.AllFilter all = 1;
    iota.grpc.v0.common.AddressFilter sender = 2;
    iota.grpc.v0.common.TransactionDigestFilter transaction = 3;
    iota.grpc.v0.common.MoveModuleFilter move_module = 4;
    iota.grpc.v0.common.MoveEventTypeFilter move_event_type = 5;
    iota.grpc.v0.common.MoveEventModuleFilter move_event_module = 6;
    TimeRangeFilter time_range = 7;
  }
}

// Filter by timestamp range
message TimeRangeFilter {
  uint64 start_time = 1;       // Start time in milliseconds since epoch (inclusive)
  uint64 end_time = 2;         // End time in milliseconds since epoch (exclusive)
}
