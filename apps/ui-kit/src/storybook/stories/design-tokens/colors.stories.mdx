import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';
import {
    IOTA_PRIMITIVES_COLOR_PALETTE,
    IOTA_NAMES_COLOR_PALETTE,
    SHADER_COLOR_PALETTE,
} from '../../../lib/constants/colors.constants';
import { COLOR_GRADIENTS } from '../../constants/gradients.constants';
import { DocsSyncTheme } from '../components';
import { formatGradientNames } from '../../helpers/formatGradientNames';

<DocsSyncTheme />
<Meta title="Design Tokens/Colors" />

## Primitives

<ColorPalette>
    {Object.entries(IOTA_PRIMITIVES_COLOR_PALETTE).map(([color, values]) => {
        return <ColorItem key={color} title={color} colors={values} />;
    })}
</ColorPalette>

## Names

<ColorPalette>
    {Object.entries(IOTA_NAMES_COLOR_PALETTE).map(([color, values]) => {
        return <ColorItem key={color} title={color} colors={values} />;
    })}
</ColorPalette>

## Gradients

<ColorPalette>
    {Object.entries(COLOR_GRADIENTS).map(([color, values]) => {
        return <ColorItem key={color} title={color} colors={formatGradientNames(color, values)} />;
    })}
</ColorPalette>

## Shaders

<ColorPalette>
    {Object.entries(SHADER_COLOR_PALETTE).map(([color, values]) => {
        return <ColorItem key={color} title={color} colors={values} />;
    })}
</ColorPalette>
